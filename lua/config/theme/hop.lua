local colors = require("config.theme.colors")

return {
    HopNextKey = { fg = colors.magenta, bold = true },
    HopNextKey1 = { fg = colors.lightBlue, bold = true },
    HopNextKey2 = { fg = colors.blue },
    HopUnmatch = { fg = colors.gray, italic = true }
}
